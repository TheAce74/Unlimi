export type Department = {
  image: string;
  sku: string;
  name: string;
  title: string;
  description: string;
  brand: number;
  price: number;
  quantity: number;
  size: number;
};

export type Departments = Department[];
